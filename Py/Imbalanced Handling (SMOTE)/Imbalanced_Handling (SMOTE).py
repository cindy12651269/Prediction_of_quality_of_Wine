# -*- coding: utf-8 -*-
"""Imbalanced_Handling (SMOTE).py

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1oTJaI7DGALy4q7VwSAFMOYLuU4fGQA-D
"""

from imblearn.over_sampling import SMOTE

X = wine.drop(columns=['quality'])
y = wine['quality']

# Apply SMOTE to balance the data via oversampling
smote = SMOTE(random_state=42)
X_resampled, y_resampled = smote.fit_resample(X, y)

# Convert the resampled data back to DataFrame and merge
wine_balanced = pd.concat([pd.DataFrame(X_resampled, columns=X.columns), pd.DataFrame(y_resampled, columns=['quality'])], axis=1)

# Check the class distribution after balancing
print(wine_balanced['quality'].value_counts())