# -*- coding: utf-8 -*-
"""Data_Type_Conversion.py

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1oTJaI7DGALy4q7VwSAFMOYLuU4fGQA-D
"""

# Divide quality into 2 categories: 'good' and 'bad'

bins = (0, 6.5, 10)
group_names = ['bad', 'good']

# Ensure 'quality' column is of numeric type before applying pd.cut
wine['quality'] = pd.to_numeric(wine['quality'], errors='coerce')

wine['quality'] = pd.cut(wine['quality'], bins = bins, labels = group_names)

from sklearn.preprocessing import LabelEncoder
label_quality = LabelEncoder()
wine['quality'] = label_quality.fit_transform(wine['quality'])

# Data after imbalance handling: SMOTE
wine_balanced['quality'].value_counts().plot.bar()