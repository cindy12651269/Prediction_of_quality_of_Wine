# -*- coding: utf-8 -*-
"""Train_the_model.py

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1oTJaI7DGALy4q7VwSAFMOYLuU4fGQA-D
"""

from sklearn.ensemble import RandomForestClassifier
from sklearn.model_selection import cross_val_score

rfc = RandomForestClassifier(n_estimators=20, random_state=42)
rfc_eval = cross_val_score(estimator=rfc, X = X_train, y =y_train, cv = 5)
display(rfc_eval)

rfc.fit(X_train, y_train)
pred_rfc = rfc.predict(X_test)

from sklearn.metrics import classification_report, accuracy_score

# Print classification report
print(classification_report(y_test, pred_rfc))

# Print accuracy score
print("Accuracy:", accuracy_score(y_test, pred_rfc))

from sklearn.linear_model import LogisticRegression

# Use the 'liblinear' solver
clf = LogisticRegression(solver='liblinear', max_iter=500, random_state=42).fit(X_train, y_train)
pred_clf = clf.predict(X_test)

# Print classification report
print(classification_report(y_test, pred_clf))